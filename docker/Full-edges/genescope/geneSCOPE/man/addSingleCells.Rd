% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_data_construction.R
\name{addSingleCells}
\alias{addSingleCells}
\title{Add single-cell spatial data.}
\usage{
addSingleCells(
  scope_obj,
  xenium_dir = NULL,
  cosmx_root = NULL,
  platform = c("auto", "Xenium", "CosMx"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{scope_obj}{A `scope_object` to extend.}

\item{xenium_dir}{Optional Xenium run directory.}

\item{cosmx_root}{Optional CosMx project root.}

\item{platform}{Platform hint (`auto`, `Xenium`, or `CosMx`).}

\item{verbose}{Emit progress messages when TRUE.}

\item{...}{Additional arguments passed to the platform-specific loader.
For backward compatibility, `data_dir` can be supplied here as an alias for
`xenium_dir` / `cosmx_root` (auto-detected when possible).}
}
\value{
Updated `scope_object` with single-cell layers.
}
\description{
Attaches platform-specific single-cell layers (CosMx or Xenium) to an existing
`scope_object`.
}
\examples{
\dontrun{
scope_obj <- createSCOPE(data_dir = "/path/to/output_dir", grid_length = 30)
scope_obj <- addSingleCells(scope_obj, data_dir = "/path/to/xenium_run")
}
}
\seealso{
`createSCOPE_xenium()`, `createSCOPE_cosmx()`
}
