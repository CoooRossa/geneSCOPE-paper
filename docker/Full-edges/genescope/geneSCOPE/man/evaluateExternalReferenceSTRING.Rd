% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_external_reference.R
\name{evaluateExternalReferenceSTRING}
\alias{evaluateExternalReferenceSTRING}
\title{Evaluate external reference concordance using STRING (CamelCase API).}
\usage{
evaluateExternalReferenceSTRING(
  x,
  grid_name = "grid30",
  stats_layer = "LeeStats_Xz",
  graph_slot_name = "g_consensus",
  graph_slot_mh = "g_morisita",
  edge_weight = "weight",
  species = 9606,
  string_score_threshold = 700,
  precision_k = c(50L, 100L, 500L),
  cache_dir = NULL,
  keep_details = FALSE,
  verbose = getOption("geneSCOPE.verbose", TRUE),
  ...
)
}
\arguments{
\item{x}{A \code{scope_object}, an \code{igraph}, or a list returned by \code{plotDendroNetwork()}.}

\item{grid_name}{Grid layer name for \code{scope_object} input (default \code{"grid30"}).}

\item{stats_layer}{Stats layer name for \code{scope_object} input (default \code{"LeeStats_Xz"}).}

\item{graph_slot_name}{Graph slot name for \code{scope_object} input (default \code{"g_consensus"}).}

\item{graph_slot_mh}{Optional MH graph slot name (default \code{"g_morisita"}).}

\item{edge_weight}{Edge attribute used for ranking edges (default \code{"weight"}).}

\item{species}{NCBI taxonomy ID for STRING (default 9606).}

\item{string_score_threshold}{STRING combined score threshold for positives.}

\item{precision_k}{Vector of K values for precision@K.}

\item{cache_dir}{Optional directory for caching STRING mappings/interactions.}

\item{keep_details}{Whether to retain edge-level annotations in the output.}

\item{verbose}{Emit progress messages when TRUE.}

\item{...}{Reserved for future extensions.}
}
\value{
A list with \code{metrics}, \code{coverage}, \code{params}, and \code{notes}.
}
\description{
Scores gene-gene edges against STRING and reports AUROC, AUPRC, precision@K,
enrichment statistics, and coverage metrics.
}
\details{
STRING is used as an external reference for positives; coverage and scores can
reflect database biases and should be interpreted accordingly.
}
\examples{
\dontrun{
score <- evaluateExternalReferenceSTRING(scope_obj, grid_name = "grid30")
}
}
