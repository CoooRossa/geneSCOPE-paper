% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal_misc.R, R/internal_plotting.R
\name{.plot_idelta}
\alias{.plot_idelta}
\title{Plot Idelta}
\usage{
.plot_idelta(
  scope_obj,
  grid_name,
  cluster_col,
  top_n = NULL,
  min_genes = 1,
  nrow = 1,
  point_size = 3,
  line_size = 0.5,
  label_size = 2,
  subCluster = NULL
)

.plot_idelta(
  scope_obj,
  grid_name,
  cluster_col,
  top_n = NULL,
  min_genes = 1,
  nrow = 1,
  point_size = 3,
  line_size = 0.5,
  label_size = 2,
  subCluster = NULL
)
}
\arguments{
\item{scope_obj}{A \code{scope_object} containing \code{@grid} and
\code{@meta.data}.}

\item{grid_name}{Character. Grid sub-layer name. If \code{NULL} and
only one layer exists, that layer is used.}

\item{cluster_col}{Column name in \code{meta.data} that assigns each
gene to a cluster.
raw or normalised Iδ.}

\item{top_n}{Optional integer. Keep the top \code{n} genes
(highest Iδ) per cluster.}

\item{min_genes}{Integer. Minimum number of genes a cluster must keep
after filtering (default 1).
unnamed the order is taken as-is; otherwise names
are matched to cluster labels.}

\item{nrow}{Integer. Number of facet rows to aim for.}

\item{point_size}{Numeric. Aesthetics for the plot.}

\item{line_size}{Numeric. Aesthetics for the plot.}

\item{label_size}{Numeric. Aesthetics for the plot.}

\item{subCluster}{Optional character vector. Restrict to these cluster
labels.}

\item{fill_col}{Parameter value.}

\item{outline_col}{Parameter value.}

\item{seed}{Random seed.}
}
\value{
A `ggplot` object (or a list of plot components) used internally.

A \code{ggplot} object.
}
\description{
Internal helper for `.plot_idelta`.

Creates a faceted scatter-line plot showing Morisita's Iδ values for the
  top genes in each cluster of a chosen grid layer.  Clusters can be
  down-sampled, re-ordered to balance facet rows, and coloured by a custom
  palette.
}
\keyword{internal}
