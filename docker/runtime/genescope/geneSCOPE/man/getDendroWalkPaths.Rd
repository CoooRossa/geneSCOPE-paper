% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_clustering.R
\name{getDendroWalkPaths}
\alias{getDendroWalkPaths}
\title{Enumerate dendrogram walk paths.}
\usage{
getDendroWalkPaths(
  dnet_obj,
  gene,
  gene2 = NULL,
  all_shortest = FALSE,
  cutoff = 8,
  max_paths = 50000,
  verbose = getOption("geneSCOPE.verbose", TRUE)
)
}
\arguments{
\item{dnet_obj}{A dendrogram network list (typically produced by clustering/plotting helpers).}

\item{gene}{Primary gene identifier to trace.}

\item{gene2}{Optional second gene identifier (two-ended walk mode).}

\item{all_shortest}{Deprecated placeholder; retained for API compatibility.}

\item{cutoff}{Maximum walk length to consider when enumerating simple paths.}

\item{max_paths}{Maximum number of paths to return (guards combinatorial explosion).}

\item{verbose}{Emit progress messages when TRUE.}
}
\value{
A list containing `paths` (character vectors) and `paths_df` (summary data.frame).
}
\description{
Extracts walk paths from a dendrogram network object produced by geneSCOPE
clustering. This is commonly used for downstream plotting/inspection.
}
\examples{
\dontrun{
scope_obj <- clusterGenes(scope_obj, grid_name = "grid30", pct_min = "q50", n_start = 100, consensus_thr = 0.9, resolution = 0.5)
dnet_obj <- plotDendroNetwork(scope_obj, grid_name = "grid30", cluster_name = "gene_cluster", graph_slot_name = "g_consensus")
paths <- getDendroWalkPaths(dnet_obj, gene = "ACTA2", cutoff = 6)
head(paths$paths_df)
}
}
\seealso{
`plotDendroNetwork()`
}
