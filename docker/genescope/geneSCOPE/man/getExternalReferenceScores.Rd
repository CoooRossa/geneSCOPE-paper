% Generated by hand for Task71
\name{getExternalReferenceScores}
\alias{getExternalReferenceScores}
\title{Extract stored external reference scores}
\usage{
getExternalReferenceScores(
  x,
  grid_name = "grid30",
  stats_layer = "LeeStats_Xz",
  graph_slot = "g_consensus",
  ref = "STRINGdb"
)
}
\arguments{
\item{x}{A \code{scope_object} or a list containing \code{external_reference}.}
\item{grid_name}{Grid layer name for \code{scope_object} input.}
\item{stats_layer}{Stats layer name for \code{scope_object} input.}
\item{graph_slot}{Graph slot name for \code{scope_object} input.}
\item{ref}{External reference name.}
}
\value{
A data.frame with one row per graph (or empty if no stored scores are found).
}
\description{
Reads the stored STRING evaluation payload and flattens it into a data.frame for
reporting or rebuttal tables.
}
\examples{
\dontrun{
score_df <- getExternalReferenceScores(scope_obj, grid_name = "grid30")
}
}
