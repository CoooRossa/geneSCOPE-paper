% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_metrics_tests.R
\name{computeGeneModuleWithinBetweenTest}
\alias{computeGeneModuleWithinBetweenTest}
\title{Compute within/between module test.}
\usage{
computeGeneModuleWithinBetweenTest(
  jac_mat,
  gene_modules,
  alternative = c("greater", "two.sided", "less"),
  method = c("wilcoxon", "permutation"),
  n_perm = 10000L,
  seed = NULL,
  min_within = 1L,
  min_between = 1L
)
}
\arguments{
\item{jac_mat}{Jaccard similarity matrix.}

\item{gene_modules}{Named vector assigning genes to modules.}

\item{alternative}{Alternative hypothesis (`greater`, `two.sided`, `less`).}

\item{method}{Testing method (`wilcoxon` or `permutation`).}

\item{n_perm}{Number of permutations when `method = "permutation"`.}

\item{seed}{Optional seed for reproducibility.}

\item{min_within}{Minimum within-module pairs required per gene.}

\item{min_between}{Minimum between-module pairs required per gene.}
}
\value{
Invisibly returns a list with per-gene summaries and a test result.
}
\description{
Compares within-module versus between-module Jaccard similarity distributions
using Wilcoxon testing or a sign-flip permutation test.
}
\examples{
set.seed(1)
genes <- paste0("g", 1:10)
jac <- matrix(runif(100), nrow = 10, dimnames = list(genes, genes))
diag(jac) <- 1
mods <- setNames(rep(c("A", "B"), each = 5), genes)
res <- computeGeneModuleWithinBetweenTest(jac, mods, method = "wilcoxon")
}
\seealso{
`computeGeneGeneJaccardHeatmap()`
}
