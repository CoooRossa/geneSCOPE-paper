% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_external_reference.R
\name{evaluateModuleQuality_STRING}
\alias{evaluateModuleQuality_STRING}
\title{Evaluate module quality with STRING and graph metrics.}
\usage{
evaluateModuleQuality_STRING(
  x,
  membership = NULL,
  grid_name = "grid30",
  stats_layer = "LeeStats_Xz",
  graph_slot = "g_consensus",
  edge_weight = "weight",
  species = 9606,
  string_score_threshold = 700,
  n_perm = 200,
  perm_mode = "label_shuffle",
  seed = 1,
  verbose = getOption("geneSCOPE.verbose", TRUE),
  ...
)
}
\arguments{
\item{x}{A \code{scope_object}, an \code{igraph}, or a list returned by \code{plotDendroNetwork()}.}

\item{membership}{Optional membership vector (named by gene).}

\item{grid_name}{Grid layer name for \code{scope_object} input (default \code{"grid30"}).}

\item{stats_layer}{Stats layer name for \code{scope_object} input (default \code{"LeeStats_Xz"}).}

\item{graph_slot}{Graph slot name for \code{scope_object} input (default \code{"g_consensus"}).}

\item{edge_weight}{Edge attribute used for weighting (default \code{"weight"}).}

\item{species}{NCBI taxonomy ID for STRING (default 9606).}

\item{string_score_threshold}{STRING combined score threshold for positives.}

\item{n_perm}{Number of permutations for within/between delta.}

\item{perm_mode}{Permutation mode (\code{label_shuffle} only).}

\item{seed}{Random seed for permutations.}

\item{verbose}{Emit progress messages when TRUE.}

\item{...}{Reserved for future extensions.}
}
\value{
A list with \code{summary}, \code{per_module}, and \code{notes}.
}
\description{
Computes within- vs between-module STRING score differences and a classical
graph metric (weighted modularity) for cluster/module assignments.
}
\details{
The within/between STRING score delta provides an objective, quantifiable
external reference for module coherence.
}
\examples{
\dontrun{
mod_eval <- evaluateModuleQuality_STRING(scope_obj, grid_name = "grid30")
}
}
\seealso{
\code{evaluateExternalReference_STRING()}
}
