% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_data_construction.R
\name{addScopeHistology}
\alias{addScopeHistology}
\title{Attach histology assets to a scope object.}
\usage{
addScopeHistology(
  scope_obj,
  grid_name,
  png_path,
  json_path = NULL,
  level = c("lowres", "hires"),
  crop_bbox_px = NULL,
  roi_bbox = NULL,
  coord_type = c("visium", "manual"),
  scalefactors = NULL,
  y_origin = c("auto", "top-left", "bottom-left")
)
}
\arguments{
\item{scope_obj}{A `scope_object` to annotate.}

\item{grid_name}{Grid layer name to update.}

\item{png_path}{Path to the histology PNG.}

\item{json_path}{Optional Visium scalefactors JSON; used when `scalefactors` is not provided.}

\item{level}{Image resolution level (`lowres` or `hires`).}

\item{crop_bbox_px}{Optional pixel-space bounding box to crop the PNG before attaching.}

\item{roi_bbox}{Optional ROI bounding box metadata stored alongside the image.}

\item{coord_type}{Coordinate system for scaling (`visium` or `manual`).}

\item{scalefactors}{Optional list of scalefactors to use instead of the JSON file.}

\item{y_origin}{Coordinate origin policy (`auto`, `top-left`, `bottom-left`).}
}
\value{
Updated `scope_object` with histology metadata.
}
\description{
Adds Visium-style histology images and scalefactors to an existing grid layer
within a `scope_object`.
}
\examples{
\dontrun{
scope_obj <- createSCOPE(data_dir = "/path/to/output_dir")
scope_obj <- addScopeHistology(scope_obj, grid_name = "grid55", png_path = "/path/to/spatial/tissue_hires_image.png", json_path = "/path/to/spatial/scalefactors_json.json")
}
}
\seealso{
`plotGridBoundary()`, `createSCOPE_visium()`
}
