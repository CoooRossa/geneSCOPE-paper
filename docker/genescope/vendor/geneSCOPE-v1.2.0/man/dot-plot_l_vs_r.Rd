% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/internal_plotting.R
\name{.plot_l_vs_r}
\alias{.plot_l_vs_r}
\title{Plot L Vs R}
\usage{
.plot_l_vs_r(
  scope_obj,
  grid_name,
  pear_level = c("cell", "grid"),
  lee_stats_layer = "LeeStats_Xz",
  delta_top_n = 10,
  flip = TRUE
)

.plot_l_vs_r(
  scope_obj,
  grid_name,
  pear_level = c("cell", "grid"),
  lee_stats_layer = "LeeStats_Xz",
  delta_top_n = 10,
  flip = TRUE
)
}
\arguments{
\item{scope_obj}{A \code{scope_object} containing both Lee's L matrices
(in \code{LeeStats_Xz}) and Pearson correlation
matrices.}

\item{grid_name}{Character. Grid sub-layer name.}

\item{pear_level}{Character, \code{"cell"} or \code{"grid"}; which
Pearson matrix to use.}

\item{lee_stats_layer}{Character. Name of the Lee's L stats layer to use
(default "LeeStats_Xz").}

\item{delta_top_n}{Integer. How many extreme \code{Delta} pairs to label
on the plot.}

\item{flip}{Logical. If \code{TRUE}, put Pearson on the x-axis and
Lee's L on the y-axis.}
}
\value{
A `ggplot` object (or a list of plot components) used internally.

A \code{ggplot} object.
}
\description{
Internal helper for `.plot_l_vs_r`.

Builds a scatter plot comparing Lee's spatial correlation statistic
  (L) with the conventional Pearson \(r\) for every gene pair in a
  selected grid layer or at the cell level.  Optionally flips axes and
  annotates the largest positive and negative L–rplotLvsR differences
  (\code{Delta}).
}
\seealso{
\code{\link{.get_top_l_vs_r}}
}
\keyword{internal}
